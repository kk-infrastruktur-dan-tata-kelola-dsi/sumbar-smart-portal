# Google Gemini API key for AI assistant route.
# Use a server-side key (no HTTP referrer restriction) with Generative Language API enabled.
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Enable map debug logging (set to 1 to enable)
# NEXT_PUBLIC_MAP_DEBUG=1
